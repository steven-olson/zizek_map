class EpubUtils:
    """
    Helper class containing all utilities to work with (load/read/etc) and epub file.
    This is important as all of our books that we'll work with are in the epub format
    """

    @staticmethod
    def load_epub(epub_path: str):
        """
        Given some path to an epub file, load that file into a variable, return it.
        :param epub_path:
        :return:
        """

    @staticmethod
    def show_available_epubs():
        """Given some directory path, show all available epub files there"""